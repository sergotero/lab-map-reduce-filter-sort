# Lab Map Reduce Filter & Sort

Steps:

$ cd lab-map-reduce-filter-sort
$ npm install
$ npm run test:watch
The test requirements (tests/map.spec.js) will guide you in solving the remaining test requirements.